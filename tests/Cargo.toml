[package]
name = "idlc_test"
description = "Sanity check for idlc changes"
version = "0.1.0"
edition = "2021"

[build-dependencies]
cc = "1.0.90"

[features]
default = ["std"]
std = []

[package.metadata.workspaces]
independent = true
